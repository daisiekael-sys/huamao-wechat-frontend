// 这是一个 web-view 壳小程序：所有界面由工作台 H5 提供
// 使用前提（缺一不可）：
//   1. 企业/非个人主体的小程序（个人主体不支持 web-view）
//   2. H5 域名已 ICP 备案（备案满 24h），并在小程序后台「业务域名」配置校验文件
//   3. config.js 里的 h5Url 已替换为你的工作台 HTTPS 地址
App({
  onLaunch() {
    console.log('贡献者工作台微信入口启动');
  },
});
